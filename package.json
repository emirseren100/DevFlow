{
  "name": "devflow",
  "version": "0.1.0",
  "private": true,
  "description": "Issue and sprint management application for small software teams",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently --names client,server --prefix-colors cyan,magenta \"npm run dev --workspace client\" \"npm run dev --workspace server\"",
    "build": "npm run build --workspace client && npm run build --workspace server",
    "typecheck": "npm run typecheck --workspace client && npm run typecheck --workspace server",
    "test": "npm run test --workspace client && npm run test --workspace server",
    "db:validate": "npm run db:validate --workspace server",
    "db:format": "npm run db:format --workspace server",
    "db:generate": "npm run db:generate --workspace server",
    "db:migrate": "npm run db:migrate --workspace server",
    "db:status": "npm run db:status --workspace server",
    "db:seed": "npm run db:seed --workspace server",
    "db:check": "npm run db:check --workspace server",
    "db:test:prepare": "npm run db:test:prepare --workspace server",
    "test:auth": "npm run test:auth --workspace server",
    "test:workspaces": "npm run test:workspaces --workspace server",
    "test:projects": "npm run test:projects --workspace server",
    "test:sprints": "npm run test:sprints --workspace server",
    "test:issues": "npm run test:issues --workspace server",
    "db:studio": "npm run db:studio --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=20.11.0"
  }
}
